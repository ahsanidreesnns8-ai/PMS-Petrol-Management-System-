# Server
PORT=5000
NODE_ENV=development

# MySQL Database
DB_HOST=localhost
DB_PORT=3306
DB_NAME=petrol_pump_db
DB_USER=root
DB_PASSWORD=ahsanidrees1044

# JWT Secret (change in production)
JWT_SECRET=your_super_secret_jwt_key_change_this
JWT_EXPIRES_IN=7d

# GST default percentage
DEFAULT_GST_PERCENT=18

# Low stock threshold (liters)
DEFAULT_LOW_STOCK_THRESHOLD=500

# File uploads
UPLOAD_MAX_SIZE=5242880
